{
  "name": "toris-desktop-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Frontend (webview) for the toris desktop app. Dev-only dependencies live here to keep the root toris-agent package at zero runtime dependencies.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
