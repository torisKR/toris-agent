node_modules/
dist/
*.log
.DS_Store
.toris-test/
coverage/
*.tgz

# Derived native artifacts — regenerate with `npm run build:native` and
# `node scripts/pack-native.js --triple <triple>`.
build/
npm/
crates/*/target/

# Tauri desktop app build artifacts (Rust target + generated schemas).
src-tauri/target/
src-tauri/gen/
